import { HomePage } from "@/components/HomePage";

/**
 * Sarom homepage.
 *
 * Visual rhythm — deliberately alternating so the page breathes:
 *   HERO (dark, film) → brands (linen) → collections (ivory)
 *   → showroom (ivory) → films (dark) → story (dark)
 *   → CTA (peach) → footer (dark)
 *
 * The body lives in components/HomePage.tsx.
 */
export default function Home() {
  return <HomePage />;
}
